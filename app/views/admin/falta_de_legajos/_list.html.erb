<%= list_builder_for @records do |list|
      list.fecha
      list.legajo :with => :legajo, :caption => 'Legajo'
      list.legajo :with => :nombre, :caption => 'Nombre'
      list.causa
      list.con_aviso_char :caption => 'Aviso?'
      list.justificada_char :caption => 'Justificada?'
      list.descargo_char :caption => 'Descargo?'
    end %>
    

<% content_for :extras do %>
  <% filters_for do %>
    <%= all_filters %>
  <% end %>
<% end %>
