<%= list_builder_for @records do |list|
      list.fecha
      list.legajo :with => :legajo, :caption => 'Legajo'
      list.legajo :with => :nombre, :caption => 'Nombre'
      list.tipo
      list.detalle
      list.fecha_descargo
      list.descargo
    end %>

<% content_for :extras do %>
  <% filters_for do %>
    <%= all_filters %>
  <% end %>
<% end %>
    