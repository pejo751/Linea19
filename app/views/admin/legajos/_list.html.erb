<%= list_builder_for @records do |list|
      list.legajo
      list.nombre
      list.telefonos
      list.html :caption => '' do |d|
         link_to 'Direcciones', admin_legajo_direccion_de_legajos_path(d)
      end
      list.html :caption => '' do |d|
         link_to 'Documentos', admin_legajo_documento_de_legajos_path(d)
      end
      list.html :caption => '' do |f|
         link_to 'Familiares', admin_legajo_familiar_de_legajos_path(f)
      end
      list.html :caption => '' do |f|
         link_to 'Fotos', admin_legajo_foto_de_legajos_path(f)
      end
    end %>
