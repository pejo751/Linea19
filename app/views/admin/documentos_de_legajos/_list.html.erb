<%= list_builder_for @records do |list|
      list.legajo_id :align => :center
      list.legajo :with => :nombre, :caption => 'Nombre Legajo'
      list.tipo_documento
      list.descripcion
      list.vencimiento
      list.usuario_verificador
      list.url_imagen
    end %>
