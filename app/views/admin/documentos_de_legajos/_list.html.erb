<%= list_builder_for @records do |list|
      #list.legajo_id :align => :center, :caption => 'Id'
      list.legajo :with => :legajo, :caption => 'Legajo'
      list.legajo :with => :nombre, :caption => 'Nombre'
      list.tipo_documento
      list.descripcion
      list.vencimiento
      list.usuario_verificador
      list.url_imagen
    end %>
